#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "Questions.h"



/*
char **read_words(const char *input_filename, int *nPtr){

	
    char **word_list; char line[100];
    char tmp;
    int a[1] = {0};
    nPtr = &a[0];
    int size;
*/
    //write your implementation here
/*

    FILE *input_file = fopen(filename, "r");

    if (input_file == NULL) {
    	printf("ERROR: COULD NOT OPEN FILE\n");
        exit(1);
    }

    fscanf(input_file, "%d", &size);
    *nPtr = size;*/
    /*
    int m,n,i; float **pp;
    scanf( “%d%d”, &m, &n );
    word_list = malloc ( m * sizeof( char * ));
    for( i = 0; i < m; i++ )
    	word_list[i] = malloc( n * sizeof(char) );

    	for (int j=0; j<size; j++) {
    		class_list[i] = calloc(1, sizeof());
    		fscanf(input_file, "%s", &tmp);
    		*(p + i*n + j )
    	sizetmp = strlength(tmp);
    	for (int j=0; i<sizetmp; i++) {
    *(p + i*n + j )

    	}
    }

    	*sizePtr = size;
    	    *(p + i*n + j )



    fclose(input_file);

    return word_list;

}

int compare_str(const char *str1,const char *str2){*/

	/*this is a helper function that you can implement and use to facilitate your development*/

   	/*compare_str alphabetically compares two strings.
    	If str2 is alphabetically smaller than str1 (comes before str1),
    	the function returns a 1, else if str1 is smaller than str2, the
    	function returns a 0.*//*

	if ((int)str2 < (int)str1) {
		str[i]
	}

*/
	/*write your implementation here and update the return accordingly*//*
    
	return -1;

}

void sort_words(char **words, int size){*/
   
	/*write your implementation here*//*
}

void swap(char **str1, char **str2){
 
	*//*this is a helper function that you can implement and use to facilitate your development*//*
}

void sort2_words(char **words, int size){

   
*/	/*write your implementation here*//*
    
}

*/
